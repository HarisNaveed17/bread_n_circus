__pycache__/
*.py[cod]
.venv/
*.db
*.db-journal
.env
.pytest_cache/
.ruff_cache/
dist/

# `vercel pull` writes the linked project id here. Local scratch, never committed.
.vercel/
.vercel
.env*

# Local agent tooling. Skills installed here are per-developer, and
# `settings.local.json` is per-machine by name. `skills-lock.json` only pins
# what lives in .claude/skills, so it is meaningless without it.
.claude/
skills-lock.json
